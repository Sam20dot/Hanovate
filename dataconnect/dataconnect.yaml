specVersion: "v1"
serviceId: "hanovate"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hanovate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
